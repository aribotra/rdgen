services:
  rdgen:
    # use bryangerlach/rdgen:latest for the latest build
    build: .
    #image: bryangerlach/rdgen:latest
    restart: unless-stopped
    environment:
      SECRET_KEY: "django-insecure-!(t-!f#6g#sr%yfded9(xha)g+=!6craeez^cp+*&bz_7vdk61"
      GHUSER: "aribotra"
      GHBEARER: "${GHBEARER}"
      GENURL: "http://PRA65:8000"
      ZIP_PASSWORD: "Ribas2026+"
      GHBRANCH: "master"
      PROTOCOL: "http"
      REPONAME: "rdgen"
      SH_SECRET: ""
      DEBUG: "True"
    ports:
      - "8000:8000"
    dns:
      - 8.8.8.8
    volumes:
      - ./exe:/opt/rdgen/exe
      - ./png:/opt/rdgen/png
      - ./temp_zips:/opt/rdgen/temp_zips
